function calculateProduct() {
    let x = 5;

    if (true) {
        let y = 4;
        let z = 3;

        x = x * y * z
    }

    // Fix this line so that it returns 60 by multiplying x, y, and z
    return x; // Should return 60
}

module.exports = calculateProduct;
